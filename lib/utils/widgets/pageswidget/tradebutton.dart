import 'package:flutter/cupertino.dart';
import 'package:rexolut/utils/widgets/appbar.dart';

class ButtonTrade extends StatelessWidget {
  const ButtonTrade({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children:[
          // DashboardAppBar(),
          // Notifs(),
        ]
      )

    );
  }
}